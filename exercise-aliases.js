/* Progressa v6.4.11 — nomes mais reconhecíveis para exercícios */
(function(){
  if(typeof EX==='undefined')return;
  if(EX.pulldown_close){
    EX.pulldown_close.name='Puxada alta no triângulo (pegada fechada)';
    EX.pulldown_close.machine='Polia alta e pegador triângulo/V';
  }
})();
