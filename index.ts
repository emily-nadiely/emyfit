import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import OpenAI from "npm:openai";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:cors});try{const key=Deno.env.get("OPENAI_API_KEY");if(!key)throw new Error("OPENAI_API_KEY não configurada");const client=new OpenAI({apiKey:key});const body=await req.json();const response=await client.responses.create({model:"gpt-5-mini",input:[{role:"system",content:"Você é o Coach do EmyFit. Use apenas o exercício, perfil, histórico e regras fornecidos. Não diagnostique. Não invente substituições fora da biblioteca. Em dor >=5/10, sintomas cardiopulmonares, inchaço ou travamento, mande interromper e buscar avaliação. Responda em português, de forma curta e explique a razão."},{role:"user",content:JSON.stringify(body)}]});return new Response(JSON.stringify({answer:response.output_text}),{headers:{...cors,"Content-Type":"application/json"}})}catch(e){return new Response(JSON.stringify({error:e.message}),{status:400,headers:{...cors,"Content-Type":"application/json"}})}});
